<odoo>
    <data>

        <record id="view_sale_order_diskon_inherit" model="ir.ui.view">
            <field name="name">view.sale.order.diskon.inherit</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='note']" position="before">
                    <group>
                        <group>
                            <field name="diskon" widget="many2many_tags" attrs="{'readonly': [('state','not in', ('draft','sent', 'waiting_for_approval'))]}" domain="['&amp;', ('state', '=', 'approved'), ('aktifkan_diskon', '=', True), ('kelas_customer_id', '=', kelas_customer_id)]"/>
                            <field name="kelas_customer_id" invisible="1"/>
                            <field name="discount_lines" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1" groups="adireksa_discount.group_diskon_per_so_line1"/>
                            <field name="discount_amount" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                            <!-- <field name="multi_discount_info" colspan="2" nolabel="1" widget="discount_multi"/> -->
                        </group>
                    </group>
                </xpath>

                <!-- <xpath expr="//group[@name='sale_total']/field[@name='amount_untaxed']" position="after">
                    <field name="discount_lines" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1" groups="adireksa_discount.group_diskon_per_so_line1"/>
                    <field name="discount_amount" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                    <field name="multi_discount_info" colspan="2" nolabel="1" widget="discount_multi"/>
                </xpath> -->

                <xpath expr="//field[@name='order_line']/tree/field[@name='tax_id']" position="after">
                    <field name="diskon_ids" widget="many2many_tags" groups="adireksa_discount.group_diskon_per_so_line1"/>
                    <field name="diskon_total" options="{'currency_field': 'currency_id'}" groups="adireksa_discount.group_diskon_per_so_line1" invisible="1"/>
                </xpath>

            </field>
        </record>

    </data>
</odoo>