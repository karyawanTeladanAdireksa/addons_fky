<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<record id="view_assign_account_invoice" model="ir.ui.view">
		<field name="name">account.assign.invoice</field>
		<field name="model">account.assign.invoice</field>
		<field name="arch" type="xml">
			<form string="Assign Invoices">
				<group>
					<field name="payment_id" invisible="1"/>
				</group>
				<field name="move_lines">
					<tree string="Invoice to Pay" editable="bottom" create="false">
	    				<field name="move_line_id" string="Move #" invisible="1"/>
	    				<field name="invoice_id" string="Invoice #" readonly="1"/>
	    				<!-- <field name="move_currency_id" string="Curr #" invisible="1"/> -->
	    				<field name="name" readonly="1"/>
	    				<field name="date" readonly="1"/>
	    				<field name="date_due" readonly="1"/>
	    				<field name="type" readonly="1" invisible="1"/>
	    				<field name="payment_currency_id" invisible="1"/>
	    				<field name="amount_total" options="{'currency_field': 'payment_currency_id'}" readonly="1"/>
	    				<field name="currency_id" invisible="1"/> 			
	    				<field name="residual" options="{'currency_field': 'payment_currency_id'}" readonly="1"/>
	                    <!-- <field name="to_reconcile"/>
	    				<field name="amount_to_pay" sum="Total Allocation" options="{'currency_field': 'currency_id'}"/>
	    				<field name="payment_difference"/>
	                    <field name="writeoff_account_id"/> -->
	    			</tree>
				</field>
				<footer>
					<button name="confirm_invoice" type="object"
						string="Assign Invoices" class="oe_highlight" />
					<button special="cancel" string="Cancel" />
				</footer>
			</form>
		</field>
	</record>

	<record id="act_assign_account_invoice" model="ir.actions.act_window">
		<field name="name">Assign Invoices</field>
		<field name="type">ir.actions.act_window</field>
		<field name="res_model">account.assign.invoice</field>
		<field name="view_mode">form</field>
		<field name="context">{}</field>
		<field name="target">new</field>
	</record>

	<!-- <record id="view_account_payment_line_tree_aos" model="ir.ui.view">
		<field name="name">account.payment.line.tree</field>
		<field name="model">account.payment.line</field>
		<field name="arch" type="xml">
			<tree string="Invoice to Pay" editable="bottom" create="false">
				<field name="move_line_id" string="Move #" invisible="1" />
				<field name="invoice_id" string="Invoice #" readonly="1" />
				<field name="move_currency_id" string="Curr #" invisible="1" />
				<field name="name" readonly="1" />
				<field name="date" readonly="1" />
				<field name="date_due" readonly="1" />
				<field name="type" readonly="1" invisible="1" />
				<field name="payment_currency_id" invisible="1" />
				<field name="amount_total"
					options="{'currency_field': 'payment_currency_id'}" readonly="1" />
				<field name="currency_id" invisible="1" />
				<field name="residual"
					options="{'currency_field': 'payment_currency_id'}" readonly="1" />
				<field name="to_reconcile" />
				<field name="amount_to_pay" sum="Total Allocation"
					options="{'currency_field': 'currency_id'}" />
				<field name="payment_difference" />
				<field name="writeoff_account_id" />
			</tree>
		</field>
	</record>

	<record id="act_assign_account_invoice" model="ir.actions.act_window">
		<field name="name">Assign Invoices</field>
		<field name="type">ir.actions.act_window</field>
		<field name="res_model">account.payment.line</field>
		<field name="view_mode">form</field>
		<field name="context">{}</field>
		<field name="target">new</field>
	</record> -->
</odoo>
