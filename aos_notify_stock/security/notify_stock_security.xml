<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data noupdate="1">

        <!-- Record Rule -->
        <!-- <record id="notify_stock_record_rule" model="ir.rule">
            <field name="name">Notify Stock Record Rule</field>
            <field name="model_id" ref="model_stock_notify_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[]</field>
        </record> -->

        <record model="ir.rule" id="stock_notify_report_comp_rule">
            <field name="name">Notify Stock Report multi-company</field>
            <field name="model_id" ref="model_stock_notify_report"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
        </record>

    </data>
</odoo>
